{% if this.url %}
    <a href="{{ this.url }}" class="{{ this.classes }} text-decoration-none" style="background-color: {{ this.color }};">{{ this.role }}</a>
{% else %}
    <span class="{{ this.classes }}" style="background-color: {{ this.color }};">{{ this.role }}</span>
{% endif %}