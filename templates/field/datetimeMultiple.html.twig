{% if field.value %}
	{% for value in field.value %}
		<time datetime="{{ value|date('c') }}" title="{{ value|date('r') }}">{{ value|format_datetime('long', 'short') }}</time>{% if not loop.last %}<br >{% endif %}
	{% endfor %}
{% else %}
	{{ include(ea.templatePath('label/empty')) }}
{% endif %}